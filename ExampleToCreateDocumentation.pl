%!  matrix_new(+Type,+Dimension,+Elements,-M1)

% Create a new matrix. In the matrix it must 
% be specified Type (ints,floats,doubles) and 
% the dimension with NRows and NColumns. The matrix 
% will be initialised with elements specified in a list.

matrix_new(doubles, [NRows, NColumns], ListElements, 
        matrix(doubles, [NRows, NColumns], Matrix)) :-

        CODICE... .
